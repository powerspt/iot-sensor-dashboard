# 실시간 센서 대시보드 — 파이썬 서버 실행에 필요한 패키지
# 설치:  pip install -r requirements.txt
flask>=3.0
# (참고) sqlite3 · secrets · datetime 등은 파이썬 표준 라이브러리라 별도 설치가 필요 없습니다.
# 방법 1 대시보드 차트는 순수 자바스크립트로 그리므로 인터넷 없이도 동작합니다.
